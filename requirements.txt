# Core scientific stack (from-scratch numpy implementations)
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
h5py>=3.8
Pillow>=9.5
imageio>=2.30

# PyTorch (CPU) — used for the framework-based assignments
# (ResNet, U-Net, YOLO decode, style transfer, Transformer)
torch>=2.0

# Testing
pytest>=7.0
