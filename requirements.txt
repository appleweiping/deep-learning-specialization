# Core scientific stack (from-scratch numpy implementations)
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
h5py>=3.8
Pillow>=9.5
imageio>=2.30

# PyTorch (CPU) — used for the framework-based assignments
# (ResNet, U-Net, face-recognition, style transfer, NMT+attention, Transformer)
torch>=2.0
torchvision>=0.15   # pretrained VGG19 for neural style transfer

# Testing
pytest>=7.0
